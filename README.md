# Pedro Site

This project contains a simple webpage to calculate heart rate zones.

## Running tests

The project uses [Jest](https://jestjs.io/) for unit testing. To run the tests:

```bash
npm install
npm test
```

The tests are located in the `tests` directory and verify the output of `updateZones()` for different heart rates.
